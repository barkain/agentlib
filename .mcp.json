{
  "mcpServers": {
    "agentlib": {
      "command": "uv",
      "args": ["run", "--directory", "${CLAUDE_PLUGIN_ROOT}", "python", "server.py"],
      "env": {
        "AGENTLIB_DATA": "${CLAUDE_PLUGIN_DATA}/library",
        "CLAUDE_PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
